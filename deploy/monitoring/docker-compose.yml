services:
  api:
    build:
      context: ../..
    image: heart-mlops:latest
    environment:
      - MODEL_PATH=/app/artifacts/model/model.joblib
    ports:
      - "8000:8000"

  prometheus:
    image: prom/prometheus:v2.54.1
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana:11.4.0
    ports:
      - "3000:3000"
    depends_on:
      - prometheus
